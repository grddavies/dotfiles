{{- /* chezmoi:modify-template */ -}}
{{- /* we have to use a modify-template to keep the token out of the managed state */ -}}
[gdrive]
type = drive
client_id = {{ (rbw "rclone-gdrive").data.username }}
client_secret = {{ (rbw "rclone-gdrive").data.password }}
scope = drive
team_drive = 

